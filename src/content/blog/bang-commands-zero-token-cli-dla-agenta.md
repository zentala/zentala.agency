---
title: 'Bang Commands: A Zero-Token CLI for Your Agent (and for You)'
date: '2026-08-08'
category: 'DevEx'
excerpt: "Not every 'skill' an agent has needs to cost tokens. Deterministic work with no decision in it — open a file, count worktrees, check a secret is set — doesn't need a model. We moved that work into plain scripts and call them like shell commands."
authorRole: 'DevEx Consultant'
published: false
series: 'agent-native-harness'
part: 5
linkedinPost: "Not every 'skill' an AI agent has needs to burn tokens.\n\nOpening a file, counting worktrees, checking a secret is set — deterministic work, no decision inside it. We moved that work into plain scripts and call them like shell commands: the agent runs one cheap call with no skill load, and I run the exact same thing myself with a `!name` prefix for zero model tokens.\n\nSmall idea, big share of daily use. Part 5 of the agent-native harness series."
---

## Part 5 of the agent-native harness series

The result: the agent does it in one cheap call with no skill load, and I run the same thing myself with a `!name` prefix for zero model tokens. This is one of the most-touched parts of the whole setup, and it weighs as much as a `.sh` file.

*(Full article coming soon.)*
