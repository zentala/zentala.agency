---

---

<div class="banner banner-devex">
  <h3>Interested in DevEx?</h3>
  <p>Check out more articles about Developer Experience:</p>
  <a href="/category/DevEx" class="banner-link">See more →</a>
</div>

<style lang="scss">
  .banner {
    margin: 2rem 0;
    padding: 1.5rem;
    background: black;
    border: 1px solid #1f2937; /* border-gray-800 */
  }

  .banner-devex {
    border-left: 4px solid #0077cc;
  }

  h3 {
    @apply text-white text-lg font-semibold mb-2;
  }

  p {
    @apply text-gray-400;
  }

  .banner-link {
    @apply inline-block mt-4 text-gray-300 hover:text-gray-100;

    &:hover {
      cursor: pointer;
      background: transparent;

      &::after {
        content: '';
        position: absolute;
        left: -1px;
        bottom: 2px;
        width: 100%;
        height: 8px;
        background-color: currentColor;
        opacity: 0.2;
        z-index: -1;
      }
    }
  }
</style>
