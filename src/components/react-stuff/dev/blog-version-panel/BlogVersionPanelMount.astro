---
import { BlogVersionPanel } from './index'

interface Props {
  collection: string
  slug: string
}

const { collection, slug } = Astro.props
const isDev = import.meta.env.DEV
---

{isDev && <BlogVersionPanel client:idle collection={collection} slug={slug} />}
