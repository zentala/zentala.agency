---
import Tag from './Tag.astro'

export interface TagGroupProps {
  tags: string[]
  class?: string
  tagClass?: string
  theme?: 'pill' | 'simple' | 'label'
  prefix?: string
  hrefPrefix?: string
}

const {
  tags,
  class: className = '',
  tagClass = '',
  theme = 'pill',
  prefix = '#',
  hrefPrefix,
} = Astro.props as TagGroupProps

const wrapperClass = `flex flex-wrap gap-1.5 ${className}`.trim()
---

<div class={wrapperClass}>
  {
    tags.map((tag: string) => (
      <Tag
        text={tag}
        prefix={prefix}
        href={hrefPrefix ? `${hrefPrefix}${tag}` : undefined}
        theme={theme}
        class={tagClass}
      />
    ))
  }
</div>

