---
import CardContainer from './CardContainer.astro'
import CardBackgroundIcon from '../primitives/CardBackgroundIcon.astro'

export interface OfferCardProps {
  number: string
  title: string
  description: string
  icon: string
  href?: string
}

const { number, title, description, icon, href } = Astro.props
---

<CardContainer href={href} class="offer-card">
  <CardBackgroundIcon icon={icon} />

  <!-- Large number in top-left corner like before -->
  <span class="number absolute left-4 top-0 text-9xl font-semibold opacity-5">
    {number}
  </span>

  <h3 class="offer-card__title">{title}</h3>
  <p class="offer-card__description">{description}</p>
</CardContainer>

<style lang="scss">
  @use '../../styles/components/cards' as *;

  .offer-card {
    position: relative;
    overflow: hidden;
  }

  .number {
    z-index: 0;
  }

  .offer-card__title {
    @extend %card-title-large;
    margin-top: 0.5rem;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }

  .offer-card__description {
    @extend %card-content;
    font-size: 1.25rem;
    color: #9ca3af;
    position: relative;
    z-index: 1;
  }

  /* Responsive typography */
  @media (max-width: 770px) {
    .number {
      font-size: 7rem;
    }

    .offer-card__title {
      font-size: 1.5rem; /* text-2xl */
      margin-bottom: 0.75rem;
    }

    .offer-card__description {
      font-size: 1.125rem; /* text-lg */
    }
  }

  @media (max-width: 480px) {
    .number {
      font-size: 5rem;
    }

    .offer-card__title {
      font-size: 1.25rem; /* text-xl */
      margin-bottom: 0.5rem;
    }

    .offer-card__description {
      font-size: 1rem; /* text-base */
    }
  }
</style>
