# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local build output logs
build.log

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
.playwright-mcp/

# Ad-hoc screenshots in repo root
/*.png

# Local caches
/.cache/

# Don't index SpecStory auto-save files, but allow explicit context inclusion via @ references
.specstory/**
.cursorindexingignore

nul
# Hook artifacts (auto-regenerated; don't commit)
.claude/.last-done-nudge
.claude/.last-cleanup
.claude/.auto-build.log
.claude/.auto-install.log
.claude/.error-counts.tmp
.plan/.error-counts.tmp

# Git worktrees (parallel branch checkouts, not branch content)
.claude/worktrees/
